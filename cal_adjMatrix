% 导入数据为矩阵pts
% 第一列LAT,第二列LON

% 形成两两之间对应的矩阵（对称阵，可以只看上三角或下三角）
[LA1,LA2]=meshgrid(pts(:,1));
[LO1,LO2]=meshgrid(pts(:,2));
 
% 计算两两之间的距离，单位为公里
dist= distance(LA1,LO1,LA2,LO2,almanac('earth','wgs84'));
%设置基站覆盖范围km
R=0.6;
%得出邻接矩阵
adj=dist>R;
adj=double(adj)
%输出到excel表格
xlswrite('adj.xls',adj);
